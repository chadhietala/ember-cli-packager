module.exports = {
  name: 'ember-qunit',
  included: function(app) {
    app.import(app.bowerDirectory + '/ember-qunit-notifications/passed.png', {
      type: 'test',
      destDir: 'assets'
    });
    app.import(app.bowerDirectory + '/ember-qunit-notifications/failed.png', {
      type: 'test',
      destDir: 'assets'
    });
  }
};
